name: Build and Push Docker Images

on:
  push:
    branches: [ "master", "main", "12.1.*" ]
    paths-ignore:
      - 'docs/**'
      - '.vscode/**'
      - '.env.example'
      - '.gitignore'
      - 'CHANGELOG'
      - 'docker-compose*'
      - 'LICENSE.txt'
      - 'README.md'
      - 'run.sh'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service: [appserver, dbaccess, licenseserver, mssql, postgres, smartview]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Setup Resources (Download Binaries)
        run: |
          chmod +x scripts/build/setup.sh
          ./scripts/build/setup.sh ${{ matrix.service }}

      - name: Build Image
        run: |
          chmod +x ${{ matrix.service }}/build.sh
          bash ${{ matrix.service }}/build.sh auto

      - name: Push to Docker Hub
        env:
          PUSH_LATEST: ${{ github.ref_name == 'master' || github.ref_name == 'main' }}
        run: |
          chmod +x ${{ matrix.service }}/push.sh
          bash ${{ matrix.service }}/push.sh
