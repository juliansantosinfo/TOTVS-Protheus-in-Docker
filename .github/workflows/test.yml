name: Test Docker Access

on:
    workflow_dispatch:

jobs:
    debug-docker:
        runs-on: self-hosted
        steps:
            - name: Debug Docker access
              run: |
                  whoami
                  id
                  ls -l /var/run/docker.sock || true
                  docker version || true
